export class BHSrc {
  defaultSource: any;
  filteredSource: any;

  constructor(defaultSource: any, filteredSource: any) {
    this.defaultSource = defaultSource;
    this.filteredSource = filteredSource;
  }
}
